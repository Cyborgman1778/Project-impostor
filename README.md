## 🚀 Puesta en Marcha (Setup)

Sigue estos pasos para tener el servidor corriendo localmente.

### 1. Requisitos

Asegúrate de tener [Node.js](https://nodejs.org/) instalado en tu sistema.

### 2. Instalación de Dependencias

Una vez que hayas clonado o hecho `pull` del repositorio, debes instalar todas las librerías necesarias.

Abre la terminal en la carpeta principal del proyecto (`/mi-backend-realtime`) y ejecuta:

```bash
npm install